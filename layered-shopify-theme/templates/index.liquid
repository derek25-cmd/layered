{%- comment -%}
  This template intentionally renders no visible content.
  The Layered React SPA handles all routing and rendering via #root.
  Shopify requires an index.liquid template, but the React app takes over.
{%- endcomment -%}
